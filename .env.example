# ───────── admin/.env.example ─────────

# Base URL of your Server API
VITE_API_BASE_URL=http://localhost:3000

# WebSocket (for live call/transcript events)
VITE_WEBSOCKET_URL=ws://localhost:3000

# (Optional) If admin ever needs direct DB access:
# VITE_MONGO_URI=${MONGO_URI}
